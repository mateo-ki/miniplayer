# 视频功能对齐发现

## Dart 项目行为
- 视频列表缓存有效期 1 小时，最多保留 20 个持久缓存。
- 缓存键由请求类型、站点 URL、分类/搜索参数和页码组成。
- 列表支持强制刷新、滚动加载下一页、按视频 ID 去重、请求代际防止旧响应覆盖新页面。
- 详情页包含收藏、继续播放、元信息、简介、多线路选集。
- 每条线路每 50 集分段，可切换正序/倒序。
- 播放器包含进度记录、自动下一集、播放中选集抽屉、倍速、音量和全屏。
- 短视频页是独立沉浸式播放器：上下滑动、点按播放暂停、重载、下一条、音量、失败换源、预取后续 5 条、缓存池指示。

## C++ 当前能力
- 已有视频列表、分类、搜索、详情补全和请求序列隔离，但无持久缓存和追加分页。
- 已有多线路选集、下载、仅保存 M3U8、播放列表、上一集/下一集、历史、倍速、音轨、字幕和全屏。
- 详情选集没有 50 集分段、正倒序、收藏和继续播放按钮。
- 短视频仍是站点卡片页面，虽有自动切换保护，但没有 Dart 风格的沉浸式切换与预取缓存池。

## 技术决策
- 视频列表缓存放在 `VideoSearchModel`，使用 `QSettings` 持久化，避免把网络模型状态分散到 QML。
- 列表追加由模型负责按 `vodId` 去重；QML 只负责触底触发。
- 收藏和继续播放优先复用现有 `QSettings`/历史模型，不另建数据库。
- 短视频预取使用临时缓存目录和 `QNetworkAccessManager`，播放时仍交给 mpv。

