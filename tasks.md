# 远程加载站点任务

## Bee 蜜蜂菜单接入任务（增补待确认）

1. [x] 完成 Bee 文档与真实接口契约核对，记录搜索、详情和推荐响应格式。
2. [x] 新增 `BeeClient`，实现 GET、超时、AES 解密和错误映射。
3. [x] 新增 `BeeVideoModel`，实现搜索、详情和选集状态。
4. [x] 新增 `BeeView.qml`，实现搜索栏、结果网格和选集播放。
5. [x] 扩展 `NavigationSidebar.qml` 和 `Main.qml`，接入 Bee 页面索引。
6. [ ] 新增推荐列表请求：`film-recommend-model/select` + `Accept: text/plain` + 直接 Base64 AES 解密。
7. [ ] 扩展 Bee 模型字段并合并列表项与详情数据，确保详情保留海报和元信息。
8. [ ] 将 Bee 简化详情替换为复用 `VideoDetailPanel.qml` 的普通视频详情布局。
9. [ ] 将 Bee 搜索栏改为响应式布局，空搜索时恢复推荐列表。
10. [ ] 修复 CMake 的 JPEG/WebP 插件传递依赖部署，并补充安装包完整性检查。
11. [ ] 增加 Bee 推荐、搜索、详情合并和页面静态集成检查。
12. [ ] 执行 QML 检查、编译、测试，并在窄窗口/最大化窗口实际验证默认列表、海报、卡片点击、详情和选集播放。

1. [x] 提取可复用的站点分享串文本导入逻辑。
2. [x] 在 `ApiSiteModel` 实现固定地址的异步下载、超时、大小限制和状态信号。
3. [x] 在站点页顶部增加“加载站点”按钮及加载中状态。
4. [x] 补充模型单元测试和播放路由集成检查。
5. [x] 执行编译、类型/静态检查和测试，失败时最多修复三轮。
6. [x] 完成站点页面静态检查和真实网络请求验证；调试版窗口验证受已运行安装版单实例限制。

## 站点排序与优质标记

1. [x] 扩展 `ApiSite`、模型角色和本地配置，支持优质标记。
2. [x] 扩展加密分享导入导出，兼容优质标记和旧格式。
3. [x] 实现模型安全移动，保持当前选中站点及关联状态一致。
4. [x] 在完整站点列表中增加拖动手柄、拖动排序和禁用条件。
5. [x] 在站点行增加优质徽标和标记切换控件。
6. [x] 增加排序、持久化、分享兼容及界面集成测试。
7. [x] 执行编译、单元测试、集成检查和 QML 编译验证；窗口交互验证受已运行安装版单实例限制。

## JSON 视频站点导入

1. [x] 增加固定远程 JSON 下载状态、超时和大小限制。
2. [x] 解析 `name` / `api` 数组并统一导入为视频站点。
3. [x] 实现 JSON 内部及现有站点 URL 去重，并保留优质标记与顺序。
4. [x] 导入后自动检测所有有效唯一站点。
5. [x] 在站点页顶部增加“JSON 导入”按钮和结果提示。
6. [x] 增加解析、去重、默认类型和检测触发测试。
7. [x] 执行编译、单元测试、真实网络测试、集成检查和 QML 验证。
## 站点检测体验修复

1. [x] 将站点页的模型状态变化改为单行同步，保留列表滚动与代理状态。
2. [x] 为 JSON 导入和“检测全部”增加最多 4 个请求的并发队列。
3. [x] 将主窗口短视频站点缓存更新从通用 `dataChanged` 拆分为站点内容专用信号。
4. [x] 执行编译、单元测试、真实网络测试、集成检查和 QML 检查。

## 站点拖动体验修复

1. [x] 移除与 `ListView` 代理布局冲突的直接目标拖动。
2. [x] 使用拖动手柄捕获、视觉位移和释放提交实现稳定排序。
3. [x] 增加列表顶部和底部边缘自动滚动。
4. [x] 执行 QML 检查、应用编译和集成验证。
## 站点批量删除

1. [x] 在模型层增加按勾选状态一次性删除站点的接口。
2. [x] 维护当前站点索引、检测状态、优质标记和持久化数据。
3. [x] 在站点列表操作区增加“删除选中”按钮及结果提示。
4. [x] 增加模型测试并执行编译、QML 和集成检查。

## 隐藏 JSON 导入

1. [x] 移除站点页面顶部的 JSON 导入按钮。
2. [x] 执行 QML、集成和模型测试检查。
## 优质站点置顶

1. [x] 在模型层实现优质站点稳定置顶。
2. [x] 在新增、加载、导入和优质标记切换后应用分组规则。
3. [x] 限制拖动排序不能跨越优质与普通分组。
4. [x] 更新测试并执行编译、QML 和集成检查。
## 下拉标记与拖动落点

1. [x] 将视频站点下拉的星号替换为柔和绿色圆形勾选标记。
2. [x] 按目标行中线计算拖动插入槽位。
3. [x] 修正向下拖动的索引偏移。
4. [x] 执行 QML、模型和集成验证。
## 乱码与控制栏热区

1. [x] 修复语音页面接口名称、报时参数和状态分隔符乱码。
2. [x] 修复表情页面标题、搜索按钮和错误状态判断乱码。
3. [x] 将普通视频控制栏显示热区从 16px 扩大到 32px。
4. [x] 执行 QML、集成和应用编译检查。
## 普通视频手势与短视频恢复

1. [x] 增加位于原生 mpv 窗口上方的普通视频透明手势层。
2. [x] 接入双击播放/暂停和长按 3 倍速、松开恢复。
3. [x] 进入短视频前保存普通视频会话。
4. [x] 离开短视频后恢复 URL、进度和播放状态。
5. [x] 执行控制器编译、测试、QML 和集成检查。
# 视频类型二级分类任务

1. [x] 扩展 `VideoSearchModel` 分类解析，保留 `type_pid`。
2. [x] 在 `SearchView` 中拆分一级分类和当前一级类型的二级分类。
3. [x] 增加第二行分类控件及一级、二级选中状态。
4. [x] 处理站点切换、搜索、“全部”和分类刷新时的状态重置。
5. [x] 增加模型解析与页面结构的回归检查。
6. [x] 执行单元测试、`qmllint`、集成检查、QML AOT 和主程序链接验证。

# 二级分类弹框交互

1. [x] 将固定第二行二级分类改为一级标签下方的小型弹框。
2. [x] 有子分类的一级标签只展开弹框，不触发视频列表请求。
3. [x] 二级标签继续使用自身 `type_id` 请求列表，单层分类保持直接筛选。
4. [x] 更新页面回归检查并执行 QML、集成和编译验证。

# 视频页面优质站点标记

1. [x] 将站点优质状态加入视频站点缓存。
2. [x] 在视频页面站点下拉行显示“优质”徽标。
3. [x] 增加回归检查并执行 QML、集成和编译验证。

# 视频播放控制栏覆盖交互

1. [x] 移除视频画面的控制栏预留高度，使控制栏覆盖显示。
2. [x] 移除控制热区的可见背景，保持热区透明。
3. [x] 鼠标离开热区或控制栏时立即隐藏，并保护进度拖动状态。
4. [x] 更新回归检查并执行 QML、集成和编译验证。

# 返回详情按钮避让导航栏

1. [ ] 修正非全屏铺满模式下返回详情按钮的横向坐标。
2. [ ] 增加导航栏避让回归检查并执行 QML、集成和编译验证。
