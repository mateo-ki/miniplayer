# 远程加载站点需求

## Bee 蜜蜂菜单接入需求（增补待确认）

### 目标
在左侧导航新增“蜜蜂”菜单，接入 `D:/project/ida/dm/bee` 文档确认的蜜蜂影视业务接口，支持搜索、详情、选集和播放。

### 功能范围
1. 新增独立 Bee 菜单，不改变现有 Dmghg 动漫菜单行为。
2. 未输入搜索词时使用 `GET https://java.beeweb.cc/film-recommend-model/select` 加载推荐视频列表，进入蜜蜂菜单即可看到内容。
3. 推荐接口使用 `Accept: text/plain`；响应为 Base64 AES 密文，解密后读取根对象的 `data` 数组。
4. 搜索使用 `GET https://java.beeweb.cc/vod/search?wd=<关键词>`；清空搜索词后恢复推荐列表，不发送空关键词搜索请求。
5. 详情使用 `GET https://java.beeweb.cc/vod/play?id=<vod_id>`。
6. 按 Bee AES-256-CBC 固定 KEY/IV 解密响应；搜索读取信封 `data` 字段，详情读取信封 `list` 字段。
7. 从 `vod_play_url` 解析 `集名$url`，显示选集并调用现有播放器播放直链。
8. 蜜蜂详情页复用普通视频菜单的详情组件和交互，包含返回、海报、年份/地区/分类/更新状态、简介、线路、选集分页和播放。
9. 详情接口不返回封面时，必须保留列表项的 `vod_pic`，不能在进入详情后丢失海报。
10. 网络错误、解密错误、空结果和无播放地址显示可读错误，不影响 Dmghg。
11. 菜单切换后保留 Bee 当前搜索词、结果、详情和选集状态。
12. Windows Debug 与 Release 构建必须完整部署 JPEG/WebP 图片插件及其传递依赖，标准 JPEG/WebP 海报均可解码。

### 暂不纳入
- Bee 账号、会员、评论、弹幕、收藏、观看上报。
- 远程播放规则自动更新和 `jx.php` 二次解析；第一版优先验证原始 Bee 直链播放。

### 验收标准
1. 左侧出现“蜜蜂”入口并能进入 Bee 页面。
2. 不输入关键词进入页面时，自动显示 Bee 推荐列表，不使用硬编码搜索词。
3. 输入任意有效关键词可显示 Bee 返回的搜索结果；清空关键词后恢复推荐列表。
4. 推荐列表和搜索结果卡片均显示海报、名称和更新信息，在窄窗口中搜索控件仍可见。
5. 点开任意结果后显示与普通视频菜单一致的详情页，并保留列表海报。
6. 详情页能显示线路和选集；大量选集按普通视频详情的分页规则展示。
7. 点击剧集能调用现有播放器播放 URL。
8. Debug 与 Release 运行包均能解码接口返回的 JPEG/WebP 海报。
9. Bee 请求失败时页面显示具体错误；动漫菜单仍可正常使用。

## 背景

站点管理页当前支持手动维护站点以及从剪贴板导入加密站点分享串。需要在页面上半部分增加一个入口，直接加载项目维护的远程站点文件：

`https://gitee.com/mateo-ki/melo-box-app/raw/master/vod.txt`

仓库页面地址为：

`https://gitee.com/mateo-ki/melo-box-app/blob/master/vod.txt`

## 功能需求

1. 在站点管理页顶部操作区增加“加载站点”按钮。
2. 点击按钮后异步下载远程 `vod.txt`，不得阻塞界面。
3. 文件内容必须按现有 `MINIPLAYER_SITE_SHARE_V1` 分享串格式导入。
4. 导入规则沿用现有剪贴板导入规则：
   - 相同 URL 的站点更新名称、地址和类型信息。
   - 不存在的站点追加到列表。
   - 无效、重复或无法解析的数据跳过。
5. 导入成功后立即刷新站点列表并保存到本地配置。
6. 加载期间按钮不可重复点击，并显示明确的加载状态。
7. 下载失败、HTTP 状态异常、内容为空或分享串无效时，在站点页显示错误信息，不修改现有站点。
8. 正常的剪贴板导入、站点编辑、删除、检测和分享功能不得受到影响。

## 站点排序与优质标记

1. 站点列表支持通过拖动改变站点顺序。
2. 拖动完成后立即持久化，应用重启后保持新顺序。
3. 移动站点时必须同步保持当前选中站点、分享勾选状态和可用性检测状态。
4. 仅在搜索内容为空且类型筛选为“全部类型”时允许拖动排序。
5. 每个站点支持手动切换“优质站点”标记。
6. 优质标记在列表中显示独立徽标，但默认不自动改变排序。
7. 优质标记写入本地配置，并随加密分享内容导出和导入。
8. 旧配置和不含优质字段的旧分享内容按普通站点兼容加载。

## JSON 视频站点导入

1. 站点页顶部增加“JSON 导入”按钮。
2. 固定加载以下远程 JSON：
   `https://gh-proxy.com/https://raw.githubusercontent.com/YYDS678/uzVideo/main/video_sources_default.json`
3. JSON 为对象数组，站点名称读取 `name`，站点地址读取 `api`。
4. 所有导入项统一作为视频站点。
5. 按规范化 URL 去重；已有 URL 更新名称并保留优质标记和当前位置，新 URL 追加。
6. JSON 内部重复 URL 只处理一次。
7. 导入后自动检测 JSON 中所有有效唯一站点的存活状态。
8. 网络错误、HTTP 错误、超限、空文件或 JSON 格式错误时不修改站点列表。

## 验收标准

1. 点击“加载站点”后可以从固定远程地址导入站点。
2. 重复点击并重复导入不会产生相同 URL 的重复站点。
3. 加载过程有可见状态，加载结束后按钮恢复可用。
4. 网络或数据错误有可读提示，原有站点数据保持不变。
5. 模型测试、集成检查、编译检查和站点页面验证通过。
6. 拖动站点后顺序立即更新，当前选中站点不发生错误切换。
7. 优质标记可以切换、持久化，并在分享导入导出后保持。
## 站点检测体验修复

1. 单个站点检测只更新对应行，不清空或重建整个站点列表。
2. JSON 导入后的自动检测使用有限并发，避免大量请求同时发出导致可用站点误判失败。
3. “检测全部”和 JSON 导入后的检测最多同时请求 4 个站点，完成一个后再启动下一个。
4. 检测状态变化不得触发短视频站点缓存重建。

## 站点拖动体验修复

1. 在未启用搜索和类型筛选时，站点拖动手柄必须可以稳定捕获鼠标并移动对应站点行。
2. 拖动到列表顶部或底部边缘时，列表必须持续自动滚动，允许跨越当前可视区域排序。
3. 取消拖动时不得改变站点顺序，完成拖动后继续保持站点状态与当前选中项一致。
## 站点批量删除

1. 站点列表操作区提供“删除选中”按钮，删除范围使用现有站点勾选状态。
2. 批量删除一次性更新模型，保留未删除站点的顺序、优质标记和检测状态。
3. 当前站点未被删除时保持当前站点；当前站点被删除时自动选择相邻的保留站点。
4. 未勾选站点时给出提示；不得删除全部站点，至少保留一个。

## JSON 导入入口

1. 站点页面不显示 JSON 导入按钮。
2. 模型层保留 JSON 下载、去重和存活检测能力，不删除已有实现。
## 优质站点置顶

1. 所有优质站点必须排列在普通站点之前。
2. 优质组内部和普通组内部保持原有相对顺序。
3. 新增、导入、读取配置或切换优质标记后立即恢复优质站点置顶顺序。
4. 手动拖动只能在站点所属分组内调整顺序，普通站点不能排在优质站点之前。
## 站点下拉选中标记与拖动落点

1. 视频页站点下拉中的当前选中项使用克制的绿色圆形勾选标记，不使用突兀的星号。
2. 拖动站点时，指针位于目标行上半区则插入其前方，位于下半区则插入其后方。
3. 向下拖动时必须补偿原站点移除造成的目标索引偏移，最终顺序与指针落点一致。
## 语音、表情文本与控制栏热区

1. 语音页面的接口名称、整点报时参数和当前语音地址分隔符必须正常显示中文。
2. 表情页面标题、搜索按钮和失败状态识别不得包含错误转码文本。
3. 普通视频播放页底部控制栏显示热区由 16px 增加到 32px，控制栏自身视觉高度不变。
## 普通视频手势与短视频切换恢复

1. 普通视频播放画面双击时切换播放和暂停。
2. 普通视频画面长按 500ms 后临时切换为 3 倍速，松开或取消时恢复原速度。
3. 原生 mpv 子窗口显示时，手势仍必须可用。
4. 从普通视频切换到短视频前保存普通视频 URL、进度和播放状态。
5. 离开短视频后恢复此前普通视频、播放进度和播放/暂停状态。
# 视频类型二级分类

1. 视频列表的类型筛选支持一级、二级两层分类。
2. 第一行显示“全部”和一级类型；点击包含二级类型的一级类型后，在该标签下方显示一个小型弹框。
3. 点击包含二级类型的一级类型时只展开弹框，不请求视频列表；点击弹框内的二级类型后，使用该二级类型自己的 `type_id` 加载列表。
4. 当前选中的一级类型和二级类型都要有明确的选中状态。
5. 切换站点、执行关键词搜索或选择“全部”时，清除不再适用的二级类型状态。
6. 对不返回父子关系或一级类型没有子类型的站点保持直接筛选行为，不影响已有分类与分页功能。

# 视频页面优质站点标记

1. 视频页面的站点下拉列表必须标明优质站点。
2. 优质标记应清晰但不刺眼，并且不能遮挡站点名称和地址。

# 视频播放控制栏覆盖交互

1. 普通视频播放画面必须铺满播放器区域，底部控制栏不得通过预留高度挤压画面。
2. 底部唤出热区本身必须完全透明，控制栏显示时覆盖在视频画面上。
3. 鼠标移出底部热区和控制栏区域后，控制栏必须立即隐藏，不等待自动隐藏计时器。
4. 拖动进度条期间保留控制栏，避免鼠标越界导致 seek 操作中断。

# 返回详情按钮避让导航栏

1. 非全屏播放状态下，“返回详情”按钮必须位于视频内容区域内，不得覆盖左侧导航菜单图标。
2. 即使开启画面铺满模式，只要左侧导航栏仍可见，按钮就必须保留导航栏宽度的横向偏移。
